/**
 * @todo
 * Step 1:Look for a binary inside of a bin folder,
 * then launch the node. If one is not provided a default substrate chain will be run.
 *
 * Step 2: Once the chain is running its time to make whatever necessary transactions need to
 * be made.
 */
